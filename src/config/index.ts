import { configuredSchuduler } from "./schedulerConfig"


export default {
    gridSize:30,
    minSnakeSpeed:4,
    maxSnakeSpeed:10,
    maxSpeedScore:20,
    shouldGoThroughWalls:false
}

export {configuredSchuduler}